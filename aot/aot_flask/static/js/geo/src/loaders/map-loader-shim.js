/**
 * Map Loader ES Module Shim
 * Imports from IIFE pattern and re-exports for bundling
 */

const AOT_MAP_LOADER = window.AOT_MAP_LOADER;

export { AOT_MAP_LOADER };
export default AOT_MAP_LOADER;
