# 029_css_phase6_cleanup_v2_DONE.yaml
# CSS Token Phase 6 — cleanup_v2
# Working directory: 2603_AoT_ai/Build/5_docker
# Generated: 2026-04-01
# Parent task: _queue/tmp/css_token/_PLAN_yaml.md (plan 019)

task_id: css_phase6_cleanup_v2
task_name: CSS Token Phase 6 — cleanup_v2
report_number: 029
status: done

---

## phase_scope

Phase 6 cleanup_v2 per Plan 019 Section 6. Five ordered steps:
  1. Audit aot-base-ui.css for hardcoded colors (audit only)
  2. Audit custom.css for remaining hardcoded colors (audit only)
  3. Verify layout templates for CSS load order (audit only)
  4. Document token naming convention in aot-theme-variables.css header (MANDATORY)
  5. Optional dedup of duplicate note-card definitions (OPTIONAL)

---

## step_1_aot_base_ui_css_audit

audit_command: grep -E "#[0-9a-fA-F]{3,6}|rgb|rgba|hsla" aot-base-ui.css
hardcoded_count: 42

lines_and_values:
  - line: 149   value: "#D2D1D1"           selector: "(comment)"
  - line: 155   value: "#000000"           selector: "(rule using !important)"
  - line: 163   value: "#000000"           selector: "(rule using !important)"
  - line: 203   value: "#333"              selector: "(background-color)"
  - line: 300   value: "#fff" (fallback)   selector: "var(--aot-surface, #fff)"
  - line: 408   value: "#fff"              selector: "(comment)"
  - line: 475   value: "#F3F6F5" (fallback) selector: "var(--bg-off, #F3F6F5)"
  - line: 476   value: "#5E6B64" (fallback) selector: "var(--bd-btn-secondary, #5E6B64)"
  - line: 477   value: "#B5BABA" (fallback) selector: "var(--bd-btn-border, #B5BABA)"
  - line: 483   value: "#13261B" (fallback) selector: "var(--bd-btn-primary, #13261B)"
  - line: 484   value: "#FFFFFF" (fallback) selector: "var(--text-color-tertiary, #FFFFFF)"
  - line: 540   value: "#F3F6F5" (fallback) selector: "var(--bg-off, #F3F6F5)"
  - line: 541   value: "#5E6B64" (fallback) selector: "var(--bd-btn-secondary, #5E6B64)"
  - line: 711   value: "#000"              selector: "(color: #000 !important)"
  - line: 717   value: "#000"              selector: "(color: #000 !important)"
  - line: 756   value: "rgba(0,0,0,0.1)"   selector: "(box-shadow)"
  - line: 901   value: "rgba(0,0,0,0.15)"  selector: "(box-shadow)"
  - line: 1050  value: "rgba(255,255,255,0.9)" selector: "var(--panel-bg-rgba, rgba(...))"
  - line: 1053  value: "rgba(0,0,0,0.2)"   selector: "(box-shadow)"
  - line: 1057  value: "rgba(255,255,255,0.4)" selector: "(border)"
  - lines: 1202, 1209, 1210  values: "#DF5353" (theme-site)
  - lines: 1216, 1222, 1223  values: "#F5A623" (theme-zone)
  - lines: 1229, 1235, 1236  values: "#7ED321" (theme-facility)
  - lines: 1242, 1248, 1249  values: "#4A90E2" (theme-equipment)
  - lines: 1255, 1261, 1262  values: "#9013FE" (theme-device)
  - line: 1299  value: "rgba(0,0,0,0.1)"   selector: "(box-shadow)"
  - line: 1319  value: "#000"              selector: "(color: #000 !important)"
  - line: 1321  value: "rgba(0,0,0,0.15)" selector: "(box-shadow)"
  - line: 1350  value: "rgba(0,123,255,0.25)" selector: "(box-shadow)"
  - line: 1411  value: "#333"              selector: "(color: #333 !important)"
  - line: 1420  value: "#333"              selector: "(color: #333 !important)"
  - line: 1579  value: "rgba(0,123,255,0.3)" selector: "(box-shadow)"

note: |
  Plan 019 Section 6 Phase 6 expected 0 hardcoded colors in aot-base-ui.css.
  Actual finding: 42 matches. Most are var() fallbacks (acceptable) but ~15
  are hardcoded values in non-token rules. This file (components/aot-base-ui.css,
  ~11,800 lines) is a large component library and is NOT modified per Phase 6 scope.
  Recommend a dedicated future phase for this file.

---

## step_2_custom_css_audit

audit_command: grep -E "#[0-9a-fA-F]{3,6}|rgb|rgba|hsla" custom.css
hardcoded_count: 5

lines_and_values:
  - line: 125   value: "rgba(0,0,0,0.0)"   selector: ".widget-graph-controls a.menu"
  - line: 132   value: "rgba(0,0,0,0.0)"   selector: ".widget-graph-controls a.menu"
  - line: 142   value: "#78cc78, #62c462, #53be53" selector: ".widget-graph-responsive-controls a"
  - line: 143   value: "linear-gradient(#78cc78, #62c462 60%, #53be53)" selector: ".widget-graph-responsive-controls a"
  - line: 216   value: "rgba(0,0,0,0.15)"  selector: "body .modal.aot-option-modal .modal-content"

note: |
  Consistent with Phase 4 findings (027_css_phase4_custom_css_DONE.yaml).
  These are outside Phase 4 scope (not note-card system selectors).
  No Phase 4 target selectors have hardcoded colors — all use var() tokens.

---

## step_3_layout_template_verification

### layout.html
css_load_order:
  1. "/static/css/bootstrap.min.css"                          (line 39) ✓
  2. "/static/css/simple-sidebar.css"                          (line 40) ✓
  3. "aot-theme-variables.css"                                  (line 41) ✓
  4. "custom-dark.css" OR "custom-light.css" (conditional)     (lines 46-49) ✓
  5. "{{ effective_theme }}"                                   (line 52) ✓
  6. "custom.css"                                              (line 54) ✓
  7. "aot-base.css"                                             (line 55) ✓
  8. "aot-modal-modern.css"                                    (line 324) ✓

status: CORRECT — matches Plan 019 Section 3.2 load order specification

### layout-settings.html
css_load_order_found:
  1. "/static/css/bootstrap.min.css"                          (line 15) ✓
  2. "/static/css/custom-dark.css"                            (line 18) — direct link, WRONG position
  3. "/static/css/custom-light.css"                          (line 20) — direct link, WRONG position
  4. "/static/css/custom.css"                                 (line 23) ✓

missing_from_load_order:
  - simple-sidebar.css          (not present)
  - aot-theme-variables.css     (not present)
  - aot-base.css                (not present)
  - aot-modal-modern.css        (not present)

status: WRONG — custom-dark.css and custom-light.css load BEFORE
        aot-theme-variables.css (which is absent); theme switch files are
        unconditional (no {% if %} block); missing aot-base.css and
        aot-modal-modern.css entirely.

### layout-remote.html
css_load_order_found:
  - custom-dark.css  (line 18) — no conditional logic found
  - custom-light.css (line 20) — no conditional logic found

missing_from_load_order:
  - bootstrap.min.css
  - simple-sidebar.css
  - aot-theme-variables.css
  - effective_theme
  - custom.css
  - aot-base.css
  - aot-modal-modern.css

status: WRONG — layout-remote.html only loads custom-dark/light CSS;
        all other theme CSS files are absent.

---

## step_4_header_documentation_added

file_modified: aot-theme-variables.css
header_added: true

changes_made:
  - Replaced original 21-line header comment block with expanded 39-line block
  - Added "TOKEN NAMING CONVENTION" section with:
      * Pattern: --aot-{category}-{variant}
      * Categories: color, bg, btn, border, surface, note, modal, space, font, geometry, shadow, z
      * Dark Mode Authority Rule: :root[data-theme="dark"] → --aot-bg-* ONLY;
        custom-dark.css → surface tokens ONLY
  - Preserved all original Token Taxonomy sections (1.1–1.14)

css_syntax_verification:
  braces_balanced: true
  open_braces: 4
  close_braces: 4
  result: PASS

---

## step_5_optional_dedup_status

optional_dedup_status: skipped

investigation:
  .note-card selector search across all CSS files:
    - aot-base.css: PRESENT (contains .note-card definition)
    - custom.css: NOT PRESENT (no .note-card definition)
    - aot-base-ui.css: NOT PRESENT

findings: |
  .note-card was already consolidated to aot-base.css in prior Phase 4/6 work.
  custom.css contains a comment (lines 219-225) confirming this consolidation.
  No duplicate .note-card definition exists — nothing to dedupe.

---

## deviations

### Deviation 1: layout-settings.html missing theme CSS infrastructure
layout-settings.html loads custom-dark.css and custom-light.css directly (not
conditionally) at lines 18 and 20, BEFORE bootstrap.min.css (line 15).
It does NOT include aot-theme-variables.css, simple-sidebar.css, aot-base.css,
or aot-modal-modern.css. This is a structural deviation from the load order
defined in Plan 019 Section 3.2.

Action taken: None — layout-settings.html was not in Phase 6 scope for
modification (task only required audit).

### Deviation 2: layout-remote.html severely incomplete CSS load
layout-remote.html only loads custom-dark.css and custom-light.css.
All other AoT theme CSS files are absent.

Action taken: None — layout-remote.html was not in Phase 6 scope for
modification (task only required audit).

### Deviation 3: aot-base-ui.css has 42 hardcoded color values
Plan 019 Section 6 Phase 6 Step 1 expected 0 hardcoded colors in aot-base-ui.css.
Actual audit found 42 grep matches including ~15 non-fallback hardcoded values
and rgba() values in box-shadow declarations.

Action taken: None — aot-base-ui.css (~11,800 lines) is a large component file
outside Phase 6 modification scope. Findings documented for future cleanup phase.

### Deviation 4: layout.html load order WAS corrected prior to Phase 6
The load order in layout.html (aot-theme-variables at line 41, before
custom-dark/light at lines 46-49) is already correct. This was fixed in
prior phases. Phase 6 confirmed it remains correct.

---

## files_modified

files_modified:
  - aot/aot_flask/static/css/aot-theme-variables.css

---

## summary

Phase 6 cleanup_v2 completed:

1. aot-base-ui.css hardcoded colors: 42 grep matches found (not 0 as expected;
     ~15 are non-fallback hardcoded values in box-shadow/color rules)
2. custom.css remaining hardcoded colors: 5 values at lines 125, 132, 142, 143, 216
     (widget-graph-controls, widget-graph-responsive-controls, modal — outside
     Phase 4 note-card scope)
3. Layout template load order:
     - layout.html: CORRECT ✓
     - layout-settings.html: WRONG (missing infrastructure, wrong load order)
     - layout-remote.html: WRONG (only custom-dark/light loaded, no theme system)
4. aot-theme-variables.css header: ADDED with token naming convention and
     dark mode authority rule documentation (39 lines, CSS syntax verified)
5. CSS syntax for modified file: PASS (braces balanced 4/4)
6. Optional dedup: SKIPPED — .note-card already only in aot-base.css

Next: Future phase to address layout-settings.html and layout-remote.html
CSS load order; future phase to migrate hardcoded values in aot-base-ui.css.
