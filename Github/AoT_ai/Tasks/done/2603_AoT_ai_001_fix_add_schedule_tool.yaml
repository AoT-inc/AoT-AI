task_id: 2603_AoT_ai_001_fix_add_schedule_tool
project_id: 2603_AoT_ai
task_name: fix_add_schedule_tool
description: 'Rewrite add_schedule_tool to call propose_job() (type=human) instead
  of creating a Notes record. AI must be able to read human schedules as context.

  '
target_path: 2603_AoT_ai/Build/5_docker
destination: 2603_AoT_ai/Build/5_docker/Tasks/done/
state: pending
created_at: '2026-03-28T00:00:00+09:00'
depends_on:
- 2603_AoT_ai_002_fix_schedule_model
prompt: "# Foreman Task — fix_add_schedule_tool\n#\n# Working directory (cwd): 2603_AoT_ai/Build/5_docker\
  \  (resolved from SYNODRIVE_ROOT)\n# Rules: read CLAUDE.md in the working directory\
  \ before any action.\n# Language: English only in all generated documents.\n# -------------------------------------------------------------------\n\
  \n## Context\nadd_schedule_tool() currently saves human work schedules as Notes\
  \ records\n(category='schedule'). This means they are never surfaced to the AI as\n\
  schedule context and are never executed or alerted.\nThe fix: route through AISchedulerService.propose_job()\
  \ with\nschedule_type=ScheduleType.human so human schedules live in SchedulerJobMeta\n\
  alongside device and AI system schedules.\n\n## Design decisions (confirmed by product\
  \ owner)\n- Human schedules are non-executable calendar entries (no APScheduler\
  \ trigger).\n- AI must be able to query all schedules (device + human + ai_system)\
  \ when\n  generating proposals, so they must be in the same SchedulerJobMeta table.\n\
  - auto-approve human schedules immediately (no human confirmation step needed\n\
  \  for self-registered human calendar entries).\n- proposed_by should reflect the\
  \ actual actor: 'AI' when the tool is called\n  by the LLM, not 'HUMAN'.\n\n---\n\
  \n## Steps\n\n### 1. Read current implementation\n1. Read `aot/ai/services/aot_data_tool_service.py`\
  \ lines 560–630\n   (add_schedule_tool function).\n2. Read `aot/ai/services/ai_scheduler_service.py`\
  \ — find propose_job()\n   and approve_job() signatures and required parameters.\n\
  3. Read the updated SchedulerJobMeta model (from task 002) to confirm\n   schedule_type\
  \ and user_id fields are present.\n\n### 2. Rewrite add_schedule_tool\nReplace the\
  \ Notes-creation logic with:\n\na. Parse input parameters — keep existing parameter\
  \ names if they are\n   already well-formed; do not change the MCP tool schema visible\
  \ to the LLM.\n\nb. Call AISchedulerService.propose_job() with:\n     - job_name:\
  \ descriptive string from user input\n     - schedule_type: ScheduleType.human\n\
  \     - proposed_by: 'AI'   ← was incorrectly 'HUMAN' before\n     - user_id: current\
  \ user id (from Flask session or context)\n     - run_at: datetime in UTC (conversion\
  \ handled by task 004; for now\n               accept the value as-is and store\
  \ it)\n     - approval_required: False   (human schedule self-registers immediately)\n\
  \     - Any other required fields: pass sensible defaults\n\nc. Immediately call\
  \ approve_job() on the returned job_id with:\n     - decided_by: 'AI'   ← consistent\
  \ with proposed_by\n     - Do NOT schedule an APScheduler trigger for human-type\
  \ schedules\n       (human schedules are calendar entries, not executable jobs)\n\
  \nd. Return a success message to the LLM confirming the schedule was registered.\n\
  \n### 3. Ensure AI can query human schedules\n3a. Search for any function that retrieves\
  \ schedules for AI context\n    (e.g., get_today_schedule, get_upcoming_schedules,\
  \ or similar).\n3b. If such a function exists and filters by type, ensure it includes\n\
  \    schedule_type=human in its query.\n3c. If no such query function exists, add\
  \ a note in the completion report\n    — do NOT create the function now (out of\
  \ scope).\n\n### 4. Clean up Notes fallback\n- Confirm no other code path creates\
  \ Notes with category='schedule' for the\n  purpose of registering a schedule.\n\
  - If found, note the location in the report; do not remove without confirming\n\
  \  scope with the operator.\n\n### 5. Verify\n- Import check: `python -c \"from\
  \ aot.ai.services.aot_data_tool_service import add_schedule_tool\"`\n- Confirm the\
  \ MCP tool schema (in mcp_aot.py or equivalent) does not need\n  updating — if it\
  \ does, update it.\n\n---\n\n## Acceptance criteria\n- add_schedule_tool no longer\
  \ writes to the Notes table for schedule entries.\n- A SchedulerJobMeta row with\
  \ schedule_type=human is created on each call.\n- proposed_by and decided_by are\
  \ set to 'AI', not 'HUMAN'.\n- Human schedules do NOT create APScheduler jobs.\n\
  - No import or syntax errors.\n\n---\n\n## Output\nWrite completion report to:\n\
  \  2603_AoT_ai/Build/5_docker/Tasks/done/fix_add_schedule_tool_COMPLETION.yaml\n\
  \nReport format:\n  task_id:              \"2603_AoT_ai_003_fix_add_schedule_tool\"\
  \n  status:               done | failed | partial\n  summary:              \"\"\n\
  \  files_modified:       []\n  notes_path_removed:   true | false\n  ai_context_query:\
  \     exists | missing | out_of_scope\n  notes:                \"\"\n\n# -------------------------------------------------------------------\n\
  # MANDATORY COMPLETION STEPS:\n# 1. Verify imports for all modified files.\n# 2.\
  \ Write the completion report above before exiting.\n# 3. task 002 (fix_schedule_model)\
  \ must be completed before this task.\n"
