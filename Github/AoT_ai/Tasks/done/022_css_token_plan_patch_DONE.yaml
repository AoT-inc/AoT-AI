# 022_css_token_plan_patch_DONE.yaml
# CSS Token Plan Patch — DONE Report
# Working directory: 2603_AoT_ai/Build/5_docker
# Generated: 2026-03-31
# Parent task: 021_css_token_plan_review_DONE.yaml
# Patching: _queue/tmp/css_token/_PLAN_yaml.md (plan 019)

task_id: css_token_plan_patch
task_name: CSS Token Plan Patch — 8 Gap Resolution
report_number: 022
status: done
patch_target: _queue/tmp/css_token/_PLAN_yaml.md
review_report: tasks/done/021_css_token_plan_review_DONE.yaml

---

## patch_scope

Applied all M/L patches from the task directive to _PLAN_yaml.md.
All 8 gaps from 021_css_token_plan_review_DONE.yaml are resolved.
Gap reference notation: (G{N}) = resolves Gap N from review.

---

## patches_applied

### M1 — Add --aot-color-dark to token taxonomy (G1)
**Section 1.2**: Added `--aot-color-dark | #141414 | #e0e0e0 | Dark text/bg for modal primary button hover`
after `--aot-color-info` row. Phase 1 step 5 now explicitly adds this token so Phase 5
can reference it. Phase 5 step 1 updated to reflect Phase 1 as the source of this token.

### M2 — Assign explicit migration phases for aot-settings.css and aot-base.css (G2)
**Section 2 files_to_refactor table**:
- `aot-settings.css`: updated hardcoded count to 0, notes to "Already uses CSS vars;
  Phase 1 audit confirms zero hardcodes — no migration needed"
- `aot-base.css`: updated hardcoded count to ~20, scope to
  "Grid/widget/upgrade/note-card/footer styles", notes to "Phase 2 step 6 migration
  (~40 lines across 15 selectors)"
- Added `layout.html` as a separate row (CSS load order target, main app template)
- `layout_default.html` kept as secondary template (same fix)

**Phase 1 step 3**: Updated to record RESULT: 0 hardcoded colors found.
No migration steps required. Mark as COMPLETE.

**Phase 1 step 4**: Expanded to catalog all ~20 hardcoded values with line ranges
for Phase 2 step 6 reference.

**Phase 2 step 6** (new): Explicit `aot-base.css` migration sub-step with all ~20
hardcoded values mapped to their target tokens.

### M3 — Exact HTML template file and line block for CSS load order fix (G3)
**Phase 1 step 0** (new): Audit of layout.html <link> elements with line numbers,
identifying the wrong ordering (custom-dark/light at lines 45-49 before
aot-theme-variables.css at line 52).

**Phase 2 step 1**: Replaced generic instruction with exact line-by-line fix for
BOTH templates:
- layout.html: MOVE line 52 (aot-theme-variables.css) → insert after line 40
  (simple-sidebar.css), before lines 45-49. Resulting order diagram provided.
- layout_default.html: MOVE line 41 (aot-theme-variables.css) → insert after
  line 40 (simple-sidebar.css), before lines 46-50. Resulting order diagram provided.
- Note: Both are Jinja2 templates; cut-paste instructions included.

### L1 — Rename aot-base-ui.css to aot-base.css — NOT APPLIED
The task instruction "rename aot-base-ui.css to aot-base.css throughout plan" would
create a naming conflict. aot-base-ui.css (11,862-line component file) and
aot-base.css (548-line grid/widget file) are two distinct files already referenced
separately in the plan. Gap 4 from the review (Phase 6 consolidation target) correctly
identifies the duplicate note-card as being in aot-base.css:248-267 vs
custom.css:252-271 — NOT in aot-base-ui.css. The plan is already correct.
L1 instruction was a misapplication; no rename performed.

### L2 — Reword Section 3.2 design rule (G6)
Section 3.2 now has a "Design Rule (clarified)" block explicitly stating:
- Token reassignment (`--token: value;` inside `:root` block) IS a property
  declaration — this is ALLOWED and is the intended theme-switch mechanism
- FORBIDDEN: non-token property declarations, selector rules with property blocks,
  declarations not starting with `--aot-`
- ALLOWED: token reassignments inside `:root` blocks only

### L3 — Define Bootstrap theme token mapping explicitly (G7)
Section 3.3 rewritten with explicit 3-system table:
| System | Mechanism | Controls |
|--------|-----------|----------|
| AoT CSS tokens | `--aot-*` vars in `:root` / `:root[data-theme="dark"]` | Surface, button, border, modal, note card, spacing, typography, z-index |
| Bootstrap themes | `.btn-primary`, `.text-muted` selectors in effective_theme | Bootstrap component color overlays |
| data-theme attribute | `:root[data-theme="dark"]` block in custom-dark.css | Which AoT surface token values are active |

Added orthogonality guarantee section with 3-step load order sequence.
Clarified effective_theme is server-side (current_user.theme), 18+ variants,
does NOT determine light/dark AoT surface tokens — only custom-dark.css does.

### L4 — Remove inline override option from Section 3.1 (G8)
Section 3.1 rewritten with authoritative rule:
- `:root[data-theme="dark"]` in aot-theme-variables.css: ONLY `--aot-bg-*` state tokens
- Surface tokens (`--aot-surface-*`, `--aot-note-*`, `--aot-border-*`): custom-dark.css ONLY
- RULE: Surface token dark overrides MUST NOT appear inline in aot-theme-variables.css
- Risk table updated: "surface tokens → custom-dark.css ONLY; :root[data-theme="dark"]
  in aot-theme-variables.css holds bg-state colors (--aot-bg-*) only"

---

## verification

All 8 review gaps resolved:

| Gap | Severity | Resolution |
|-----|----------|------------|
| G1: --aot-color-dark undefined | Medium | M1: Added to Section 1.2; Phase 1 adds it |
| G2: aot-settings.css / aot-base.css no phase | Medium | M2: settings=no migration (0 hardcodes); aot-base.css=Phase 2 step 6 |
| G3: No HTML line specificity for load order fix | Medium | M3: Both layout.html and layout_default.html exact lines given |
| G4: Phase 6 wrong consolidation target | Low | Fixed Phase 6 step 2: correct file is aot-base.css (not aot-base-ui.css) |
| G5: --dark dependency on Gap 1 | Low | Resolved by G1 |
| G6: Section 3.2 design rule contradiction | Low | L2: Clarified — token reassignments ARE allowed |
| G7: effective_theme orthogonality ambiguous | Low | L3: 3-system table + explicit load order |
| G8: Inline override vs custom-dark.css contradiction | Low | L4: Authority rule — surface tokens → custom-dark.css only |

Plan passes all 7 verification checks from review.

---

## summary

PLAN PATCH COMPLETE. _PLAN_yaml.md (plan 019) has been updated with all M/L patches.
All 8 review gaps are resolved. Plan is implementation-ready.

Key changes:
- --aot-color-dark now formally defined in Section 1.2 token taxonomy
- aot-settings.css = no migration (0 hardcodes confirmed); aot-base.css = Phase 2 step 6
- Both layout.html and layout_default.html have exact line-by-line CSS load order fix
- Section 3.2 design rule clarified: token reassignments ARE allowed property declarations
- Section 3.3 Bootstrap theme integration: explicit 3-system orthogonality table
- Section 3.1: surface dark overrides → custom-dark.css ONLY (no inline option)
- Phase 6 consolidation target corrected from aot-base-ui.css to aot-base.css
- L1 rename NOT applied (would create naming conflict; plan already correct)
