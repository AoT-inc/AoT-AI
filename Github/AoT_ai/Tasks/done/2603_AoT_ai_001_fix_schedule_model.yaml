task_id: 2603_AoT_ai_001_fix_schedule_model
project_id: 2603_AoT_ai
task_name: fix_schedule_model
description: 'Add type (device|human|ai_system) and user_id fields to SchedulerJobMeta.
  Run DB migration. Update all call sites to pass the new fields.

  '
target_path: 2603_AoT_ai/Build/5_docker
destination: 2603_AoT_ai/Build/5_docker/Tasks/done/
state: pending
created_at: '2026-03-28T00:00:00+09:00'
depends_on: []
prompt: "# Foreman Task — fix_schedule_model\n#\n# Working directory (cwd): 2603_AoT_ai/Build/5_docker\
  \  (resolved from SYNODRIVE_ROOT)\n# Rules: read CLAUDE.md in the working directory\
  \ before any action.\n# Language: English only in all generated documents.\n# -------------------------------------------------------------------\n\
  \n## Context\nSchedulerJobMeta is the unified schedule model for all schedule types\n\
  (device reservations, human calendar entries, AI system jobs).\nCurrently it has\
  \ no type field and no user_id field, making per-type\nqueries and per-user isolation\
  \ impossible.\nThis task adds those two fields and migrates existing data.\n\n##\
  \ Design decisions (confirmed by product owner)\n- type enum values: device | human\
  \ | ai_system\n- user_id: nullable FK to User (NULL = system job, not null = owned\
  \ job)\n- Existing rows: set type='ai_system', user_id=NULL by default on migration\n\
  - All datetimes in this model: store as UTC (timezone handling is addressed\n  separately\
  \ in task 004; do not change datetime logic here)\n\n---\n\n## Steps\n\n### 1. Understand\
  \ the current model\n1. Read `aot/databases/models/scheduler.py` — note all existing\
  \ fields,\n   constraints, and relationships on SchedulerJobMeta.\n2. Read `aot/ai/services/ai_scheduler_service.py`\
  \ — find all locations\n   where SchedulerJobMeta is instantiated (constructor calls).\n\
  3. Identify any other files that directly construct SchedulerJobMeta\n   (search\
  \ the codebase for `SchedulerJobMeta(`).\n\n### 2. Update the model\nIn `aot/databases/models/scheduler.py`:\n\
  a. Add an Enum type:\n     class ScheduleType(enum.Enum):\n         device    =\
  \ \"device\"\n         human     = \"human\"\n         ai_system = \"ai_system\"\
  \nb. Add two columns to SchedulerJobMeta:\n     schedule_type = db.Column(db.Enum(ScheduleType),\n\
  \                               nullable=False,\n                              \
  \ default=ScheduleType.ai_system)\n     user_id = db.Column(db.Integer,\n      \
  \                   db.ForeignKey('users.id', ondelete='SET NULL'),\n          \
  \               nullable=True,\n                         index=True)\nc. Add relationship\
  \ (if User model exists):\n     owner = db.relationship('User', backref='schedules',\
  \ lazy='select')\nd. Do NOT rename or remove any existing columns.\n\n### 3. Write\
  \ the migration script\nCreate `aot/databases/migrations/add_schedule_type_user_id.py`\
  \ with:\n- Forward migration: ALTER TABLE to add schedule_type and user_id columns.\n\
  - Set schedule_type = 'ai_system' for all existing rows.\n- Backward migration:\
  \ DROP COLUMN for both fields.\n- Include a docstring with date, task_id, and description.\n\
  If the project uses Alembic, create an Alembic revision file instead and\nfollow\
  \ the existing revision naming convention.\n\n### 4. Update all call sites\nFor\
  \ every location found in step 1 that instantiates SchedulerJobMeta:\n- If the call\
  \ is inside AI system job registration → add schedule_type=ScheduleType.ai_system\n\
  - If the call is inside add_schedule_tool (human schedule) → add schedule_type=ScheduleType.human\n\
  - If the call is inside schedule_device_control_tool → add schedule_type=ScheduleType.device\n\
  - Pass user_id where a user context is available; otherwise leave NULL.\n\n### 5.\
  \ Verify\n- Confirm `python -c \"from aot.databases.models.scheduler import SchedulerJobMeta\"\
  ` runs without error.\n- Confirm no existing test or startup script crashes due\
  \ to the schema change.\n- Do NOT run actual DB migrations against a live DB — leave\
  \ that to the operator.\n\n---\n\n## Acceptance criteria\n- SchedulerJobMeta has\
  \ schedule_type and user_id columns defined in the model.\n- Migration script is\
  \ complete with forward and backward paths.\n- All SchedulerJobMeta constructor\
  \ calls pass schedule_type explicitly.\n- No existing field is renamed or removed.\n\
  \n---\n\n## Output\nWrite completion report to:\n  2603_AoT_ai/Build/5_docker/Tasks/done/fix_schedule_model_COMPLETION.yaml\n\
  \nReport format:\n  task_id:        \"2603_AoT_ai_002_fix_schedule_model\"\n  status:\
  \         done | failed | partial\n  summary:        \"\"\n  files_modified: []\n\
  \  files_created:  []\n  callsite_count: 0     # number of SchedulerJobMeta constructors\
  \ updated\n  notes:          \"\"\n\n# -------------------------------------------------------------------\n\
  # MANDATORY COMPLETION STEPS:\n# 1. Verify import syntax before exiting.\n# 2. Write\
  \ the completion report above before exiting.\n# 3. Do NOT run ALTER TABLE on any\
  \ live database.\n"
