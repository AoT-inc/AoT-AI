task_id: 2603_AoT_ai_001_run_db_migration_schedule
project_id: 2603_AoT_ai
task_name: run_db_migration_schedule
description: 'Run the add_schedule_type_user_id migration to add schedule_type and
  user_id columns to SchedulerJobMeta. Verify columns exist after migration.

  '
target_path: 2603_AoT_ai/Build/5_docker
destination: 2603_AoT_ai/Build/5_docker/Tasks/done/
state: pending
created_at: '2026-03-28T00:00:00+09:00'
depends_on:
- 2603_AoT_ai_002_fix_schedule_model
prompt: "# Foreman Task — run_db_migration_schedule\n#\n# Working directory (cwd):\
  \ 2603_AoT_ai/Build/5_docker  (resolved from SYNODRIVE_ROOT)\n# Rules: read CLAUDE.md\
  \ in the working directory before any action.\n# Language: English only in all generated\
  \ documents.\n# -------------------------------------------------------------------\n\
  \n## Context\nTask 002 added schedule_type (device|human|ai_system) and user_id\
  \ columns\nto the SchedulerJobMeta model and created two migration scripts:\n  -\
  \ Standalone: aot/databases/migrations/add_schedule_type_user_id.py\n  - Alembic:\
  \    alembic_db/alembic/versions/c3d4e5f6a7b8_add_schedule_type_user_id_to_scheduler.py\n\
  \nNeither has been applied to the live database yet. This task runs the\nmigration,\
  \ verifies the result, and documents the outcome.\n\n---\n\n## Steps\n\n### 1. Pre-flight\
  \ checks\n1. Confirm the DB file path from environment or config\n   (e.g. DATABASE_PATH\
  \ env var, or docker-compose.yml → volumes).\n2. Back up the current scheduler DB:\n\
  \     cp <db_path>/aot_scheduler.db <db_path>/aot_scheduler.db.bak_$(date +%Y%m%d_%H%M%S)\n\
  3. Verify the backup exists before proceeding.\n\n### 2. Check Alembic availability\n\
  Run:\n  cd alembic_db && alembic current 2>&1\n\na. If Alembic is configured and\
  \ returns a current revision:\n   → use the Alembic path (Step 3A).\nb. If Alembic\
  \ is not configured or returns an error:\n   → use the standalone script path (Step\
  \ 3B).\n\n### 3A. Alembic migration (if available)\n1. Review the revision file\
  \ to confirm it targets the correct table:\n     cat alembic_db/alembic/versions/c3d4e5f6a7b8_add_schedule_type_user_id_to_scheduler.py\n\
  2. Run:\n     cd alembic_db && alembic upgrade head\n3. Confirm exit code is 0.\n\
  \n### 3B. Standalone migration (fallback)\n1. Review the script:\n     cat aot/databases/migrations/add_schedule_type_user_id.py\n\
  2. Run:\n     python3 aot/databases/migrations/add_schedule_type_user_id.py\n3.\
  \ Confirm exit code is 0.\n\n### 4. Verify columns exist\nRun a schema inspection\
  \ against the live DB:\n\n  python3 -c \"\n  import sqlite3, os\n  db = os.environ.get('SCHEDULER_DB_PATH',\
  \ 'aot_scheduler.db')\n  conn = sqlite3.connect(db)\n  cols = [row[1] for row in\
  \ conn.execute('PRAGMA table_info(scheduler_job_meta)')]\n  print('Columns:', cols)\n\
  \  assert 'schedule_type' in cols, 'MISSING: schedule_type'\n  assert 'user_id'\
  \ in cols, 'MISSING: user_id'\n  print('VERIFY OK')\n  conn.close()\n  \"\n\nIf\
  \ VERIFY OK is not printed, the migration failed — record status: failed.\n\n###\
  \ 5. Verify existing rows\nConfirm existing rows received the default backfill:\n\
  \n  python3 -c \"\n  import sqlite3, os\n  db = os.environ.get('SCHEDULER_DB_PATH',\
  \ 'aot_scheduler.db')\n  conn = sqlite3.connect(db)\n  rows = conn.execute(\n  \
  \    'SELECT count(*) FROM scheduler_job_meta WHERE schedule_type IS NULL'\n  ).fetchone()[0]\n\
  \  print(f'Rows with NULL schedule_type: {rows}')\n  if rows == 0:\n      print('Backfill\
  \ OK')\n  else:\n      print('WARNING: backfill incomplete')\n  conn.close()\n \
  \ \"\n\n### 6. Docker service check (optional)\nIf the aot-app container is running,\
  \ confirm it starts cleanly after migration:\n  docker compose logs aot-app --tail=20\
  \ 2>&1 | grep -E \"ERROR|WARNING|started\"\n\n---\n\n## Acceptance criteria\n- Backup\
  \ file exists before migration runs.\n- schedule_type and user_id columns exist\
  \ in scheduler_job_meta after migration.\n- No existing rows have NULL schedule_type\
  \ (backfill complete).\n- aot-app container has no new startup errors related to\
  \ the schema change.\n\n---\n\n## Output\nWrite a completion report to EXACT path\
  \ (relative to SYNODRIVE_ROOT):\n  2603_AoT_ai/Build/5_docker/Tasks/done/006_run_db_migration_schedule_COMPLETION.yaml\n\
  \nReport format:\n  task_id:          \"2603_AoT_ai_006_run_db_migration_schedule\"\
  \n  status:           done | failed | partial\n  summary:          \"\"\n  migration_method:\
  \ alembic | standalone\n  backup_path:      \"\"\n  verify_stdout:    \"\"     #\
  \ raw output of PRAGMA table_info check\n  backfill_rows:    0      # rows with\
  \ NULL schedule_type after migration\n  docker_errors:    \"\"     # any new ERROR\
  \ lines from aot-app logs, or \"none\"\n  notes:            \"\"\n\n# -------------------------------------------------------------------\n\
  # MANDATORY COMPLETION STEPS:\n# 1. Verify backup exists before running any migration\
  \ command.\n# 2. Run the PRAGMA table_info check and include raw stdout in the report.\n\
  # 3. Write the completion report to the EXACT path shown in ## Output above.\n#\
  \    The path must be expanded (no YAML field references like \"destination\").\n\
  #    Path is relative to SYNODRIVE_ROOT, not cwd.\n"
